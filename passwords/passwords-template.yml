# This is the dummy file with used passwords and addresses.
# Copy its structure and name it 'passwords.yml' for deployment.
# You should never upload passwords to the GitHub!

HikServer:
  address: 0.0.0.0
  password: password
  user: user